HOW TO CRACK:

1. Open "ed.exe" as Administrator [Until menu is reached]

2. Go into the folder Debugger -> and run "x64dbg.exe" [Until fully loaded]

3. Go to
  FILE (at the top of x64dbg) -> ATTATCH (and attatch "ed.exe")

4. If at the bottom left of the window the small box is RED/or/YELLOW click the top button (ARROW POINTING TO THE RIGHT) "Run" Until not RED/or/YELLOW

5. Once fully working and the Menu is not frozen, Go to
  FILE (at the top of x64dbg) -> PATCH
   -> Click on IMPORT and import "sub_patch.1337"

6. Once 3/3 Patches applied (or all patches applied) close patch menu.

[ MUST DO OR ELSE YOU WILL BE BANNED FROM NOVA ]
7. Go to
  FILE -> DETACH and click it

8. Close x64dbg and if the menu has become a Console with BLUE/CYAN Text. Then it is done and working!

9. Follow on-screen prompts to finish


[ WHEN RELOADING THE CHEAT - IF CHEAT CRASHING ]
-> Everytime you load the cheat make sure to run "clear_cache.bat" as administrator